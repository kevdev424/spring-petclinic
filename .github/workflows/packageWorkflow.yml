name: Package Pet Clinic

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
  push:
    branches: [main]
    
jobs:
  build:
    runs-on: windows-latest
    env:
      PROJECT_NAME: ${{github.repository}}
      VERSION: ${{github.ref}}
   
